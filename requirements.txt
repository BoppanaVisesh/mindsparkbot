# Pull CPU wheels first so pip avoids CUDA/NVIDIA torch dependencies.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.5.1+cpu
transformers>=4.46,<5
datasets>=4.7,<5
accelerate>=0.30,<2
fastapi>=0.110,<1
uvicorn>=0.30,<1
